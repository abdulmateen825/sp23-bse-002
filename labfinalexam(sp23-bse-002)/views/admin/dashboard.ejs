<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .hero-section {
            background: linear-gradient(90deg, #00796b, #004d40);
            color: white;
            text-align: center;
            padding: 50px 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }
        .hero-section h1 {
            font-size: 2.8rem;
            font-weight: bold;
        }
        .hero-section p {
            font-size: 1.2rem;
            font-weight: 400;
        }
        .stats-section {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 40px;
        }
        .stat-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 25px;
            text-align: center;
            width: 250px;
            transition: transform 0.2s ease-in-out;
            margin-top: 20px;
        }
        .stat-card:hover {
            transform: translateY(-10px);
        }
        .stat-card h3 {
            font-size: 1.3rem;
            color: #004d40;
            margin-bottom: 15px;
        }
        .stat-card p {
            font-size: 1.8rem;
            font-weight: bold;
            color: #00796b;
        }
        .order-dashboard {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 20px;
        }
        .order-dashboard h2 {
            font-size: 2rem;
            color: #004d40;
            margin-bottom: 20px;
        }
        .order-dashboard p {
            font-size: 1.1rem;
            margin-bottom: 15px;
        }
        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }
        table th, table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        table th {
            background-color: #004d40;
            color: white;
        }
        table tr:hover {
            background-color: #f1f8f6;
        }
        .cta-section {
            text-align: center;
            margin-top: 50px;
        }
        .cta-section h4 {
            font-size: 1.5rem;
            color: #00796b;
        }
        .cta-section p {
            font-size: 1.1rem;
            color: #333;
        }
        .cta-section .btn {
            background-color: #00796b;
            color: white;
            font-size: 1.2rem;
            padding: 15px 30px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .cta-section .btn:hover {
            background-color: #004d40;
        }
    </style>
</head>
<body>
   
    <div class="container mt-4">
        <!-- Hero Section -->
        <div class="hero-section">
            <h1>Welcome to Ideas by Gul Ahmed</h1>
            <p>Manage your store effectively with our intuitive admin dashboard.</p>
        </div>

        <!-- Statistics Section -->
        <div class="stats-section">
            <div class="stat-card">
                <h3>Total Products</h3>
                <p><%= productCount %></p>
            </div>
            <div class="stat-card">
                <h3>Total Orders</h3>
                <p><%= orderCount %></p>
            </div>
            <div class="stat-card">
                <h3>Total Categories</h3>
                <p><%= categoryCount %></p>
            </div>
        </div>

        <!-- Order Dashboard Section -->
        <div class="order-dashboard">
            <h2>Order Dashboard</h2>
            <p>Total Orders: <%= totalOrders %></p>
            <p>Processing Orders: <%= processingOrders %></p>
            <p>Shipped Orders: <%= shippedOrders %></p>
            <p>Completed Orders: <%= completedOrders %></p>
            <p>Cancelled Orders: <%= cancelledOrders %></p>
            <p>Total Revenue: $<%= totalRevenue.toFixed(2) %></p>

           
        </div>

        <!-- Call-to-Action Section -->
        <div class="cta-section">
            <h4>Need Assistance?</h4>
            <p>Contact our support team for any help you need with managing your store.</p>
            <a href="mailto:support@example.com" class="btn">Contact Support</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
